# Vercel KV 数据库环境变量
# 在 Vercel Dashboard → Storage → Create → KV 创建后自动注入
# 本地开发无需配置，会自动 fallback 到本地文件存储

# Vercel KV REST API URL
KV_REST_API_URL=

# Vercel KV REST API Token
KV_REST_API_TOKEN=

# 可选：KV URL 和 Token（部分场景使用）
KV_URL=
KV_TOKEN=
